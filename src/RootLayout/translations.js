import { defineMessages } from 'react-intl';

export const moduleName = "TaskManager.RootLayout";

export default defineMessages({
  welcome: {
    id: `${moduleName}.welcome`,
    defaultMessage: "Open up App.js to start working on your app! Bill Mb.",
  },
});
